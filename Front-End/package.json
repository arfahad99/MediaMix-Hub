{
  "name": "mediamix-hub-frontend",
  "version": "1.0.0",
  "description": "Frontend for MediaMix Hub - A modern media management application",
  "main": "index.html",
  "scripts": {
    "start": "npm run build-css && concurrently \"npm run watch-css\" \"live-server --port=3000 --host=localhost --open=/login-backend.html\"",
    "dev": "npm run build-css && concurrently \"npm run watch-css\" \"live-server --port=3000 --host=localhost --open=/login-backend.html --watch=.\"",
    "build": "npm run build-css",
    "build-css": "tailwindcss -i ./src/input.css -o ./dist/styles.css --watch=false",
    "watch-css": "tailwindcss -i ./src/input.css -o ./dist/styles.css --watch"
  },
  "keywords": [
    "media",
    "frontend",
    "vanilla-js",
    "html",
    "css"
  ],
  "author": "MediaMix Hub Team",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.0",
    "concurrently": "^8.2.2"
  }
}
